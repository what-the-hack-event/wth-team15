name: First Workflow

on: 
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Step 1"
      - name: Step 2
        run: echo "Step 2"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: Cowsay
        uses: actions/cowsay@v2
        with:
          text: "Moo"